# Spec-Gap Ledger

> Cross-project. Every row is a category of thing a spec should have caught the first time but
> didn't — discovered either (a) after a post-ship follow-up pointed it out, or (b) recurring
> across multiple Phase 2.5 review rounds on the same or different projects (broadened 2026-09-08:
> the underlying failure is the same either way — Phase 2 not asking the right question upfront —
> a post-ship miss and a review-time recurrence are just two different discovery moments for it).
> Read by `/dark-factory` phase 2 (Requirements) on every future run, before that run's own FR/NFR
> pass is called complete. Append-only — a category is marked resolved-by-convention only after 3+
> consecutive specs checked it with no repeat miss, never deleted outright. See
> `~/.claude/skills/dark-factory/SKILL.md` ("Spec-Gap Ledger" section) and `DARK-FACTORY-DESIGN.md`
> for how this fits the pipeline.

| Date | Project | What was missed | Category | Now checked in Phase 2 as | Resolved-by-convention |
|---|---|---|---|---|---|
| 2026-09-07 | nuwa-m3 | Reasoner (LLM) calls passed unit/integration/E2E tests but still failed on first real interactive use — a markdown-fence-wrapped response the tests never exercised | probabilistic-call-reliability | NFR: every LLM/probabilistic call names a real-E2E-with-the-real-model test (never fake-only), a bounded retry, and failure tracing — see dark-factory SKILL.md's NFR table | |
| 2026-09-07 | nuwa-m3 | A schema-valid, 200-OK LLM output was semantically wrong ('speeder' resolved to speedcubing, not motorcycle racing) because the prompt never stated the account's actual content domain | creative-domain-anchoring | NFR: every prompt generating creative/subjective output states the concrete domain/audience explicitly, and the spec names a judged example the reasoner must resolve correctly — see dark-factory SKILL.md's NFR table | |
| 2026-09-08 | nuwa-m3 (Design Variants FR-21..26) | The SAME failure recurred 4 times across rounds 1/4/4→5 of one feature's review: a technical claim (a "registry" is extensible, `_store` persists to disk, a field list is exhaustive, N calls run "in parallel") stated as fact in the FR/NFR text without ever being checked against the real function signature/schema/execution model it described | asserted-not-verified-claim | NFR/FR authoring rule: any claim about a DEPENDENCY's behavior (persistence, concurrency safety, exhaustiveness of a field/case list, extensibility of an existing mechanism) must name the specific line/function it was verified against, or be stated as unverified — a claim with no verification pointer is the smell | |
| 2026-09-08 | foreman | `core/runner.js`'s PR-merge success classifier (`findPr`/`classifyRun`) only ever queried GitHub for an OPEN pull request. Under `full` autonomy the dispatched agent is itself an actor that merges the PR before returning — never named as such when the classifier was first specced — so a fully-successful, fully-merged build (nuwa#3/PR #4) was silently misclassified `needs-human`, corrupting both the run ledger and the GitHub issue's label. Discovered live via the run ledger, not review | concurrent-actor-incomplete | NFR: Concurrency/race safety (dark-factory SKILL.md's NFR table) — name every actor including the system's own other processes; a success/failure classifier over an external resource must enumerate that resource's real states, never collapse a multi-state resource (open/merged/closed) into a binary check | |
| 2026-09-08 | catwalk | `GraphNode`'s TypeScript interface (`component-graph.tsx`) gained `detail`/`artifactPath` fields that were never mirrored into the matching zod runtime schema (`PhaseGraphNodeSchema`, `foreman.ts`). zod's default silently strips unknown keys, so the API response quietly dropped both fields — the feature compiled, typechecked, and rendered with no error, and the gap was caught only by live browser click-testing (clicking a node did nothing) | runtime-schema-drift | NFR: Schema/type integrity (TypeScript stacks, dark-factory SKILL.md's NFR table) — a runtime-validated field and its TS type must be updated in the same change, with a test that fails if they drift; a validator's silent-drop-vs-fail-closed behavior must be a stated choice | |
| 2026-09-08 | nuwa-m3 (issue #12, recorded demo) | A previously-fixed invariant (D90: a beat's rendered highlight window must never exceed its own `target_duration_ms`) was re-verified only on the code path it was originally found on (the auto-match highlight-window selection, `shot_matcher.py`). A second, independent code path that can set the same field — Stage 2's manual alternate-clip picker (`page.tsx`'s `onOverride`, backed by `CandidateResponse`/`override_beat`) — had no clamp at all, and a real run reproduced the exact failure shape (a 2.5s beat rendered with a 38.9s window), passing every quality gate along the way, because none of the 6 render-time quality gates check this invariant either | invariant-checked-on-one-path-only | NFR: for any invariant a spec states as a hard rule (a clamp, a uniqueness constraint, a required transform), the spec must enumerate every code path capable of producing the governed field/state — not just the one path the original bug was found on — and either share one enforcement point (preferred) or state per-path why each is independently safe; a quality-gate/test suite that only exercises the original path is a false green | |
| 2026-09-13 | nuwa | recurring false-negative: runner.js's PR lookup ('No PR found for the branch', line ~103) returned failure on 2 separate nuwa#18 runs (18-581511, 18-4a73db) despite a real PR (nuwa#35) existing and being pushed successfully -- this is what forced 3 manual re-dispatches and ultimately a manual gh pr merge instead of Foreman's own auto-merge path ever running, even though nuwa's effective autonomy was already 'full' the whole time. D22 (2026-09-08) fixed a related but distinct instance (backgrounded run_in_background orphaning the worktree before any PR existed); this is a second, still-open instance of the same failure class where a PR DOES exist but the lookup doesn't find it. Worth a dedicated look at the retry/timing window in runner.js's findPrWithRetry. | bug |  |  |
| 2026-09-14 | foreman | runner.js's claudeArgs is hardcoded to build-only framing ('Resume the pipeline from Phase 3... open a PR that closes #N') for EVERY dispatch, regardless of what the issue body actually requests. Confirmed live: issue #1 in Research-2026 explicitly asked for dark-factory 'spec-only' mode (Phase 2 formalize + Phase 2.5 review, explicit stop-condition, no PR expected) -- the dispatched session correctly detected the contradiction between runner.js's build-only prompt and the issue's own stop instruction and refused to proceed rather than building against an unformalized spec, but this wasted a real ~172s/3.3M-token run and needs a human to manually intervene every time a non-build mode is wanted. Separately, runner.js's semantic-success classifier requires an open PR to count as success -- a genuine spec-only run (docs-only, no PR by design) would ALWAYS classify as failure under current logic even if it worked perfectly. Real fix needed: runner.js (or scheduler.js before it) should read the issue body for an explicit mode directive and branch both the prompt template AND the success classifier accordingly, not just always assume build-only. | bug |  |  |
